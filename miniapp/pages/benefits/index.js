const {request}=require('../../utils/request'),{ensureLogin}=require('../../utils/auth');
const {yuan}=require('../../utils/demo-wallet');
const names={SIGNIN:'每日签到',ACTIVITY:'活动奖励',FIRST:'完成首单',TEN:'十单成就',WEEK_THREE:'本周完成3单',STREAK_THREE:'连续交付'};
Page({data:{info:null,busy:false,error:'',tab:'coupons'},onShow(){if(ensureLogin())this.load();},onPullDownRefresh(){this.load().finally(()=>wx.stopPullDownRefresh());},retry(){this.load();},more(){if(this.data.info?.nextOffset!==null)this.load(true);},async load(more=false){if(this._loading)return;this._loading=true;try{const info=await request('GET','/benefits',{offset:more?this.data.info.nextOffset:0});info.coupons=info.coupons.map(x=>({...x,amount:yuan(x.amountFen),min:yuan(x.minSpendFen)}));info.coins=info.coins.map(x=>({...x,label:names[x.taskKey]||'任务奖励'}));info.offers=info.offers.map(x=>({...x,amountText:x.kind==='COUPON'?'¥'+yuan(x.amount):x.amount+' 币'}));if(more){info.coupons=this.data.info.coupons.concat(info.coupons);info.coins=this.data.info.coins.concat(info.coins);}this.setData({info,error:''});}catch(e){this.setData({error:e.message});}finally{this._loading=false;}},tab(e){this.setData({tab:e.currentTarget.dataset.tab});},signin(){wx.navigateTo({url:'/pages/incentives/index'});},async claim(e){if(this.data.busy)return;this.setData({busy:true});try{const r=await request('POST','/benefits/claim',{key:e.currentTarget.dataset.key});wx.showToast({title:r.duplicate?'已领取，请勿重复':'已领取'});await this.load();}catch(e){wx.showToast({title:e.message,icon:'none'});}finally{this.setData({busy:false});}}});
