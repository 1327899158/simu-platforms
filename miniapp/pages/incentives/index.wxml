<view class="community"><view class="title">激励中心</view><view class="notice">按真实已完成订单统计。自然周从北京时间周一开始；连续天数按每天至少完成一单计算（今天尚未完成可延续昨天）。奖励登记为预留权益，暂不可抵扣、提现或转让，不影响现有支付。</view><view class="error" wx:if="{{error}}" bindtap="load">{{error}}</view><block wx:if="{{info}}"><view class="card"><view>本周完成 {{info.weekly}} 单 · 连续 {{info.streak}} 天</view><view>累计完成 {{info.total}} 单 · 预留奖励 {{info.reservedCoins}} 币</view><view class="muted">本周起始：{{info.period}}</view></view><view class="title">成就与任务</view><view wx:for="{{info.tasks}}" wx:key="key" class="card"><view>{{item.eligible?'🏅':'○'}} {{item.title}}</view><view class="muted">进度 {{item.value}} / {{item.target}} · 奖励 {{item.amount}} 币</view><button data-key="{{item.key}}" bindtap="claim" disabled="{{busy||!item.eligible||item.claimed}}">{{item.claimed?'已登记':item.eligible?'领取预留奖励':'尚未达标'}}</button></view><view class="title">本周完单榜 · 前20名</view><view class="card" wx:for="{{info.leaderboard}}" wx:key="id">{{index+1}}. {{item.nickname}} · {{item.completed}}单</view><view wx:if="{{!info.leaderboard.length}}" class="muted">本周暂无完成订单</view><view class="title">奖励记录（最近50条）</view><view class="card" wx:for="{{info.rewards}}" wx:key="createdAt">{{item.taskKey}} · {{item.periodKey}}<view class="muted">+{{item.amount}}币 · 预留中 · {{item.createdAt}}</view></view></block></view>
