<view class="page" wx:if="{{order}}"><view class="hero"><text class="hero-title">评价客户</text><view class="customer"><image wx:if="{{customer.avatarUrl}}" src="{{customer.avatarUrl}}"/><view><text class="customer-name">{{customer.nickname || '客户'}}</text><text class="hero-sub">{{order.projectName}} · ¥{{order.finalY}}</text></view></view></view><view class="card"><text class="prompt">这位客户的需求描述清晰吗？是否配合验收？</text><view class="big-stars"><text wx:for="{{stars}}" wx:key="*this" class="big-star {{item <= score ? 'active' : ''}}" data-score="{{item}}" bindtap="chooseStar">★</text></view><text class="score-label">{{score ? (score === 5 ? '非常满意' : score + ' 星') : '请选择评分'}}</text></view><view class="card"><text class="section-title">评价标签（可多选）</text><view class="tag-grid"><text wx:for="{{tagOptions}}" wx:key="value" class="review-tag {{item.selected ? 'selected' : ''}}" data-tag="{{item.value}}" bindtap="toggleTag">{{item.selected ? '✓ ' : ''}}{{item.value}}</text></view></view><view class="card"><text class="section-title">详细评价（客户不可见，仅作为平台治理参考）</text><view class="ui-icon-copy private-tip"><ui-symbol value="💡"/> 评价内容客户本人不可见，仅供平台内部评估本次合作情况，不会影响客户发布需求的权限</view><textarea class="comment-input" maxlength="500" value="{{content}}" bindinput="inputContent" placeholder="记录本次合作中的具体情况（可选）"/></view><button class="submit" loading="{{saving}}" disabled="{{saving}}" bindtap="submit">{{editing ? '确认修改评价' : '提交评价'}}</button></view>
